/**
 * Tokens brand phosphore — miroir de `theme.css` pour OG / icon (Satori).
 * Ne pas diverger des variables CSS `--canvas-*` / `--fg-*`.
 */
export const BRAND = {
  canvasSubtle: "#050805",
  fgDefault: "#39ff14",
  fgMuted: "#1fbf0f",
  fgSubtle: "#0f7a0a",
  phosphorRgb: "57, 255, 20",
} as const;

export const SITE_TAGLINE =
  "Turn development activity into visible progress." as const;
